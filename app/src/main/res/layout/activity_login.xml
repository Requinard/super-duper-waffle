<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorBlue"
    tools:context=".ui.activities.LoginActivity">

    <EditText
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:inputType="textPersonName"
        android:text="Naam"
        android:id="@+id/tbNaam"
        android:background="@color/colorWhite"
        android:textColor="@color/colorBlack"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp" />

    <EditText
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:inputType="textPassword"
        android:text="Wachtwoord"
        android:id="@+id/tbWachtwoord"
        android:background="@color/colorWhite"
        android:textColor="@color/colorBlack"
        android:layout_below="@+id/tbNaam"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp" />

    <Button
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Log in"
        android:id="@+id/btnLogin"
        android:layout_marginTop="20dp"
        android:background="@color/colorBlack"
        android:textColor="@color/colorBlue"
        android:layout_below="@+id/tbWachtwoord"
        android:layout_alignStart="@+id/tbWachtwoord"
        android:layout_alignEnd="@+id/tbWachtwoord" />

    <Button
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Registeer"
        android:id="@+id/btnRegisteer"
        android:layout_marginTop="10dp"
        android:background="@color/colorBlue"
        android:textColor="@color/colorBlack"
        android:layout_below="@+id/btnLogin"
        android:layout_alignStart="@+id/btnLogin"
        android:layout_alignEnd="@+id/btnLogin" />

</RelativeLayout>
